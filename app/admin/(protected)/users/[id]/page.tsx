import Link from "next/link";import { ArrowLeft } from "lucide-react";import { findAdminUser } from "@/repositories/admin-user-repository";import { AdminUserEditor } from "@/components/admin-user-editor";
export default async function AdminUserPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const user=await findAdminUser(Number(id));if(!user)return <p>ไม่พบผู้ใช้</p>;return <><Link href="/admin/users" className="inline-flex items-center gap-2 text-sm font-bold text-[var(--primary-dark)]"><ArrowLeft size={16}/>กลับไปผู้ใช้ทั้งหมด</Link><h1 className="mt-5 text-3xl font-extrabold">จัดการผู้ใช้: {user.username}</h1><AdminUserEditor user={user}/></>}
