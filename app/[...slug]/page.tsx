import { notFound } from "next/navigation";
import { SiteFooter } from "../../components/site-footer";
import { SiteHeader } from "../../components/site-header";

const pages: Record<string, { title: string; text: string }> = { news: { title: "ข่าวสารและกิจกรรม", text: "ติดตามข่าวประชาสัมพันธ์ กิจกรรม และประกาศจากราชวิทยาลัยจักษุแพทย์แห่งประเทศไทย" }, articles: { title: "บทความสุขภาพตา", text: "คลังความรู้ด้านสุขภาพตาสำหรับประชาชนและบุคลากรทางการแพทย์" }, about: { title: "เกี่ยวกับ RCOPT", text: "ราชวิทยาลัยจักษุแพทย์แห่งประเทศไทย มุ่งมั่นพัฒนามาตรฐานวิชาชีพและเผยแพร่ความรู้ด้านจักษุวิทยา" }, contact: { title: "ติดต่อเรา", text: "สอบถามข้อมูลและติดต่อราชวิทยาลัยจักษุแพทย์แห่งประเทศไทยได้ที่ admin@rcopt.org" }, search: { title: "ค้นหา", text: "ค้นหาข้อมูลบนเว็บไซต์ RCOPT" } };
export default async function GenericPage({ params }: { params: Promise<{ slug: string[] }> }) { const { slug } = await params; if (slug.length !== 1 || !pages[slug[0]]) notFound(); const page = pages[slug[0]]; return <><SiteHeader /><main className="container-shell min-h-[55vh] py-20"><p className="eyebrow mb-3">RCOPT</p><h1 className="section-title">{page.title}</h1><p className="mt-5 max-w-2xl leading-8 text-[var(--muted)]">{page.text}</p><div className="card mt-10 min-h-40 p-8 text-sm text-[var(--muted)]">หน้านี้เตรียมโครงสร้างไว้สำหรับเชื่อมต่อข้อมูลจาก legacy CMS ในระยะ read-only ถัดไป</div></main><SiteFooter /></>; }
